import { TemplateList } from '@/components/admin/whatsapp/template-list';

export const dynamic = 'force-dynamic';

export const metadata = {
    title: 'Templates — WhatsApp — Olivia Admin',
};

export default function WhatsAppTemplatesPage() {
    return <TemplateList />;
}
