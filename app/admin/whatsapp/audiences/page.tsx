import { AudienceList } from '@/components/admin/whatsapp/audience-list';

export const dynamic = 'force-dynamic';

export const metadata = {
    title: 'Audiences — WhatsApp — Olivia Admin',
};

export default function WhatsAppAudiencesPage() {
    return <AudienceList />;
}
